# MVP Experiment

Placeholder workspace for the first StateGraph MVP comparison.

Intended methods:

- `stategraph`
- `vector_rag`
- `summary_memory`
- `time_decay_rag`
- `cupmem_reimpl`

The current script does not run experiments. It only documents the intended
entry point for a future lightweight MVP run.

